/// Flutter plugin for the Erxes messenger.
///
/// Wraps the native Erxes SDKs (`MessengerSDK` on iOS,
/// `io.github.munkhorgilb:messenger-sdk` on Android) behind an idiomatic Dart
/// API. See [ErxesMessenger] for the entry point.
library;

export 'src/erxes_messenger.dart';
export 'src/models.dart';
